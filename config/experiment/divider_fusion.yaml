# @package _global_
defaults:
  - override /model: fusion_divider
  - override /data: VP
  - override /visualization: VP_viz
  - override /metrics: divier_metrics
  - override /loss: divider_loss

visualization:
  key: ['STATIC','PED','WALKWAY','CARPARK','DIVIDER']
  flip: True

model:
  is_fuse: True

data:
  pts_file: 'hidden'
  num_classes: 15
  image:
    h: 256
    w: 704
#  256×704
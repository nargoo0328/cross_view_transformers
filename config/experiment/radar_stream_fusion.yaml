# @package _global_
defaults:
  - override /model: radar_stream_fusion_model
  - override /data: nuscenes_vehicle

  - override /loss: multiclass_loss
  - override /metrics: multiclass_metrics

visualization:
  flip: False
  # key: ['STATIC','DIVIDER','bev','ped']
  key: ['bev']
    
data:
  num_classes: 15